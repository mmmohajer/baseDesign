import React from 'react';

import ActivableElement from 'Components/ActivableElement';

function TestActivableElement() {
  return (
    <>
      <ActivableElement>Activable Element</ActivableElement>
    </>
  );
}

export default TestActivableElement;
